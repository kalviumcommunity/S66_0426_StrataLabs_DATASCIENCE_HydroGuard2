# Data Directory Guide

- `raw/`: source files exactly as received.
- `processed/`: cleaned and transformed datasets used for modeling.

Keep raw inputs immutable. Store all derived datasets in `processed/`.
